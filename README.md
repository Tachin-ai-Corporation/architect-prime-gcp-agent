# Architect Prime CoreKit (public)

This repo holds the public, curlable core files needed by Architect Prime.

- Use `manifest.txt` to install files into `~/.openclaw/...`
- Pin installs to a tag (e.g. `cp004-ok`) for repeatability.
