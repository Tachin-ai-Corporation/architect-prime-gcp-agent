---
name: brain-architecture
description: Use when working on the brain agent system — creating/editing agent workspace files, updating agent-registry.json, modifying bootstrap config agent definitions, or neural gateway configuration. Also for agent routing and intent classification.
---
# Brain Architecture Implementation

## Current State (v2026.09.04.1.1)
6 brain agents in gateway multi-agent configuration, coordinated by the `agent-brain.service` state machine. Brain daemon builds `skill_index` deterministically at startup and injects into cortex payloads with `intent_keywords` for process selection. System prompt = SOUL.md only (no TOOLS.md injection). `assemble-persona` handles specialty SOUL appends at bootstrap and during `upgrade-corekit`. Motor dispatches receive full project context (`_projectContext`) and raw user request (`_sourceText`) as first-class user message sections. LoopGuard provides duplicate detection, semantic stuck detection, and structured `[STUCK REPORT]` output. Cross-agent delegation flows through checkpoint-executor with a 4-layer self-delegation prevention system: prefrontal SOUL specialty ownership detection, cortex SOUL delegation rules, inbound delegation rules, and a code-level checkpoint-executor guard that converts self-delegations to local motor tasks. Delegation dispatches via GChat @-mention with machine-parseable envelope references. Parallel delegation fan-out within a single checkpoint. Product Architect and PM agents are delegation-first (delegate before executing). Designer motor has mandatory step-by-step HTML writeFile enforcement. **Prime Unbound**: Prime is a creative system operator with `system-shell`, `gcp-admin`, and `scripting` skills, mandated to solve problems resourcefully using native tools. **Wait Capability**: All agents can invoke `wait` to pause missions for a duration; the daemon detects expired timers and resumes from the last state. **Harness Horizon**: transitions fleet agent messaging exclusively to Google Chat, retires all dashboard write paths with 405/read-only panel migrations, streamlines chronological thread assembly, establishes safe tool-verified conversational response gates (10s whitelisted REST reads timeout racing), implements tone-continuity conversation voicing context tracking (1500-char tail slice), directs all chat session triage through standard nightly memory consolidation, and visualizes deepest active descendant tasks dynamically inside the presence dashboard ribbon. **Verification & anti-foreclosure (v2026.07.20–21)**: cerebellum verifies at the **checkpoint boundary** (organs self-verify their own tasks); `verdict.mjs` extracts the full FAIL reason (no first-`)` truncation); an empty cerebellum verdict retries once before failing closed; a synthesis over-claim revises the answer (`synthesize_with_failure`), never re-plans verified work. **Brain Canon B-34 ("failures inform; they never foreclose")** is carried by temporal-memory (recalled failures → forward lessons), prefrontal (`premise: flawed` requires a named false world/state frame, never failure history), and cortex (no "capability limitation" from a tally of stumbles). **Google Docs editing is surgical/in-place** (workspace-docs v18: `docs-find-replace` / `docs-batch-edit` mixed-op reverse-ordered batchUpdate / `docs-section-delete`; formatting preserved; `docs-cat --fingerprint` before/after; `docs-replace-file` refuses formatting-destroying `.txt`/`.md`). **Inter-organ context sharing (v2026.07.22, [ORGAN_CONTEXT_SHARING_PLAN](../../../docs/plans/ORGAN_CONTEXT_SHARING_PLAN.md))**: one organ's result crosses to another as a **resource packet** — a shape-aware `summary` + a `ref` (full output on `tEnv.output`) — not a blind head+tail clip that drops list rows/tool data; cortex reads the summary in its decide delta (`buildDecideDeltaBlock`) and pulls full content by `ref` via `request_context` only when the summary is insufficient; cerebellum verifies from full evidence (B-28). On a duplicate-call loop-terminate the gateway forces a **tool-less synthesis** from gathered results instead of returning FAILURE (`corekit/brain/loop.mjs`, Google path). A per-iteration **mission blackboard** (`shared/<id>/MISSION.md`, `platform/work/blackboard.mjs`) is the git-versioned shared trail. Pure core `platform/work/result-packet.mjs`; all knobs in `contracts.json` `organ_context` (default-on, revertible). plan-structuring accept_criteria are format-agnostic (no "JSON array" for a "list" request). **Temporal-Memory is the memory authority (v2026.07.23, [TEMPORAL_MEMORY_AUTHORITY_PLAN](../../../docs/plans/TEMPORAL_MEMORY_AUTHORITY_PLAN.md))**: recall + consolidation are one job in two modes — recall stays daemon-prefetched/toolless (hot path, C-5), consolidation is the tool-executing mode temporal-memory runs itself (per-request `exec` gate in `config.mjs`: `dispatchAgent` sets `_exec:true`, recall's direct `callAgent` never does — so recall is never accidentally tool-enabled). It owns+executes the memory skills; `p-memory-consolidate` now runs on temporal-memory and emits a `consolidation_report.md`; `writeMemory` skips the consolidation mission (no self-pollution). Memory is value-weighted (B-5): `core-memory-write --importance`, and `core-memory-read` value-ranks (importance×confidence×match, recency tiebreak) so recall surfaces the good stuff first. Canon: organ table + B-3/B-5/B-16/B-17 amended; Motor is the only mutator of *external* state (temporal-memory mutates only memory); each organ owns+executes the skills of its one job. **Consolidation now completes cleanly (v2026.07.23.11.0–.12.0)** — a scheduler-fired process runs deterministically via `executeProcess` (the scheduler no longer pre-sets top-level `process_id`, which had tripped `follow_process`'s already-executed guard so the process never ran); checkpoint task dispatch sets `_exec:true` so temporal-memory is tooled during checkpoints; the singleton guard uses a fail-safe in-progress allowlist (`{active,queued,pending,waiting}`, `type==='M'`) so terminal `archived` missions don't falsely block. **On-demand responsibility triggers (v2026.07.23.8.0–.12.0, [RESPONSIBILITY_TRIGGER_PLAN](../../../docs/plans/RESPONSIBILITY_TRIGGER_PLAN.md))**: `scheduler.fireById` fires a responsibility out of turn behind two faces — a `trigger_responsibility` Cortex action (agent honors a user request; `available_responsibilities`+guidance in the decide boot block; `triggerable:true` opt-in) and an operator dashboard "Run now" (→ introspect enqueues a top-level `responsibility_triggers` doc → brain `checkResponsibilityTriggers` claims+fires). Singleton always enforced; on-demand bypasses min-spacing. No SOUL edits. **Process-as-narrative (v2026.08.14)**: processes are no longer executable step-machines — the `follow_process` action, `process-engine.mjs` step executor + plan lifecycle, the "prefer follow_process" bias, `required_processes` mandates, and the `process-manage` step-CRUD tool are all REMOVED fleet-wide. A process is now a name+description+narrative playbook RECALLED into the agent's own `checkpoint_plan` (the sole work-structuring path); `process-engine.mjs` → slim `process-registry.mjs` (loads local `corekit/config/processes/*.json` + the ROOT Firestore `processes` library, no execution). Authored via the base `process-ops` skill; temporal-memory auto-refreshes a used process/project's context on mission completion (`context_maintenance`). **Post-checkpoint brain changes (v2026.08.24–09.01)**: intake claiming is **addressed-only** (pure `agentClaimsIntake` in `platform/work/plan-utils.mjs` — a fleet agent, whose brain runs with `PRIME_ID = its managing prime` and so shares `primes/{prime}/intake`, claims ONLY intakes addressed to it; the prime owns prime-addressed + unaddressed); **work-recall is prime-scoped** to the running prime's `prime_id` (all primes share owner `prime`, so unscoped recall leaked cross-prime) and `executeFleetStatus` labels own-vs-other fleets (needs `{noOrderBy:true}` — the helper's default `orderBy:'created_at'` excludes the `createdAt`-keyed fleet docs); **agent-ears `waitForDwd()`** waits/retries instead of `exit(1)` when the agent's Workspace user doesn't exist yet (no crash-loop → no C-19 kill → auto-recovers), and the prime cortex walks the operator through creating it; **`handleApprovalResponse` writes the approval RECEIPT + resolves the intake BEFORE** the inline `_approvalChecker.checkPending()` resume (which runs the whole approved mission synchronously) so the receipt precedes the resumed mission's output; and **`project_bootstrap`** is a cortex action (enabled fleet-wide) that stands up a delivery project from a chat ask — binding the origin GChat space, seeding team/canon/context/deploy, and re-scoping the mission so the context miner curates the new project rather than `general`. **Capability posture (C-37)**: the SINGLE brain overlays a role-resolved cognitive-latitude profile onto its effective contract — Prime `unbound` (execution organs prefrontal/motor/cerebellum → `subagentStrong` gemini-2.5-pro + budget headroom: brain.max_iterations 25→40, dispatch.max_iterations 50→75), fleet `strict` (empty overlay = baseline). Pure `platform/contracts/posture.mjs` (`agentPosture`/`applyPosture`), applied INDEPENDENTLY by the daemon (`agent-brain.mjs`, before its budget const captures) and the gateway (`corekit/brain/config.mjs` `getContracts`, so `vertex.strong_model_agents` routing reflects the posture); role via the prime-only `prime-config.json` disk marker (written on Prime VMs at bootstrap). One daemon, one codebase — the posture is config resolved by role, NOT a fork; it widens cognition only, never the deterministic spine or the fence. Per-VM `AGENT_POSTURE=strict|unbound` override.

### Agent Inventory

| Agent | Model | Role | Workspace |
|-------|-------|------|-----------|
| **cortex** | gemini-3.1-pro-preview | JSON Decide Loop — envelope coordinator (DEFAULT) | `/opt/corekit/workspace` |
| **temporal-research** | gemini-3.6-flash | Web search (Vertex AI grounding) | `/opt/corekit/workspace-temporal-research` |
| **temporal-memory** | gemini-3.6-flash | Pure memory/context recall (NO external APIs) | `/opt/corekit/workspace-temporal-memory` |
| **prefrontal** | gemini-3.6-flash | Planning + dispatch (two-mode: simple + advisory) | `/opt/corekit/workspace-prefrontal` |
| **motor** | gemini-3.6-flash | Execution — ALL Google Workspace tools + advisory mode | `/opt/corekit/workspace-motor` |
| **cerebellum** | gemini-3.6-flash | Verification + validation-rule checking | `/opt/corekit/workspace-cerebellum` |

> **Model source of truth:** the per-organ model is resolved at runtime from `infra/contracts.json` → `vertex.models` (the `subagent` tier for the five subagent organs; the `cortex` tier for cortex). The registry files (`corekit/config/agent-registry*.json`) carry routes + sampling params only — not model selection. To change a model, edit `contracts.vertex.models`.

### Dispatch Pattern
- **Brain v3 state machine (`agent-brain.mjs`)**: Manages deterministic, envelope-based coordination as a continuous service.
- **Cortex JSON Decide Loop**: Cortex classifies inputs and makes structured decisions (`action: "classify"|"decide"|"short_circuit"|"dispatch"|"synthesize"`). 10-case response normalizer infers actions from fields present (e.g., `intent:"synthesize"` → synthesize, `blocker` → blocked, `result` → synthesize fallback).
- **Schema-alias normalization (`platform/providers/vertex-text.mjs` `normalizeDecision`)**: cortex reliably echoes the mode marker (`"action":"classify"`) and drifts on the real decision's field name — historically fixed for `move`→`action`, `attach_to_mission`→`attach_to`, `continue_to`→`continue_mission`; as of v2026.07.12.4.0 also `type`/bare `action`→`classification` for the classify schema (guarded against aliasing the literal `"classify"` echo itself). `enforceSchemaFn` parses+normalizes the raw response exactly once and reuses that object for both the deterministic validate and the fast-exit — do not reintroduce a second fresh `parseJsonResponse(raw)` there, or an alias applied to the first parse silently won't reach the second. The classify fast-exit checks `classification` only; `action` is never a legitimate classify field.
- **R/M/C/T Hierarchy**: Work is managed as nested envelopes of Responsibilities (cron scheduled, individually toggleable), Missions, Checkpoints, and Tasks.
- **Sub-agent Dispatch**: Sub-agents are invoked dynamically by Cortex to execute planned steps.

### Outcome Integrity
Contract-driven guarantees that dispatch and completion produce verifiable results:
- **smartTruncate**: Content truncation follows a field-priority contract (`result` → `summary` → `notes`), never silently drops outcome data.
- **Mission record persistence**: `completeEnvelope` Step 3a writes a durable mission record to Firestore before marking the envelope complete.
- **Delegation trailer grammar**: Delegation dispatches append a machine-parseable trailer (envelope ref, expected output, return address) so the receiving agent can satisfy the contract.
- **Delegation validation gates (v2026.07.12)**: `handleDelegate` validates BEFORE creating envelopes — email shape (`normalizeTargetEmail` strips trailing punctuation the intercept regex captures), self-delegation, fleet-registry online lookup, and project-has-GChat-space (delegations deliver only through shared project spaces). Rejections return actionable `[SYSTEM]` feedback naming valid agents/projects. The delegation lives under a Checkpoint (M→C→T per C-15) and its T carries `source_meta.delivery_envelope_id`; the mouth retries failed deliveries with persisted exponential backoff (`contracts.delivery`, terminal `delivery_status='failed'` at max_attempts) and `checkWaitingEnvelopes` fast-fails the waiting T off that terminal status instead of waiting out the 4h delegation timeout.
- **Prime fleet-ops skills are prime-scoped (v2026.07.12.6.0)**: mirror of the fleet-only delegation scoping. `fleet-introspect` (`roles:["prime"]`, script `fleet-work-read`) is Prime's read-only OBSERVE capability — reads any fleet agent's missions/checkpoint-task trees from the shared root `work/` collection with the metadata token, no SSH. Prime's OPERATE path (shell for test/upgrade/remediate) works via `gcloud compute ssh --tunnel-through-iap`; the enabling grant is `roles/iap.tunnelResourceAccessor` on the Prime SA (the ONLY thing missing — compute.admin already covers instance access), codified idempotently in `fleet-deploy`. When reading large Firestore payloads in a shell tool, stream to a temp file and pass the PATH — never the payload as argv (the `work-log-read` "Argument list too long" bug).
- **Delegation is fleet-only + project-scoped (v2026.07.12.3.0)**: Primes never delegate — Prime cortex SOUL mandates direct fleet operation (SSH, work-log reads, fleet-verify/fleet-upgrade). Skills can declare `roles` in skill.json; `buildSkillIndex` filters by the agent role from the VM's `/opt/corekit/corekit/STATE.json` (stale skill dirs can't resurface a role-excluded skill). The delegate action, checkpoint_plan intercept, executor delegation steps (`delegationEnabled` dep), and process-engine all gate on the delegation skill being indexed; cortex prompt surfaces omit `delegate` when unavailable. Delegation additionally requires the mission to name a registered project (gate 0.5) with a GChat space.
- **Completion verification**: `synthesize` verifies that every dispatched checkpoint has a recorded outcome before composing the final response.
- **GOAL STATE + goal_check**: Prefrontal plans include an explicit `GOAL STATE` block; cortex runs a `goal_check` pass before synthesize to confirm the goal was met.
- **Organs own the HOW, skills own the tools (v2026.07.19)**: Prefrontal decomposes by *outcome/ownership/dependency*, never tool-step, and never names commands/API ops — it sees only the skill catalog (`formatSkillCatalog`), not the command surface, so naming a nonexistent `batch_update` step was the archetypal failure. Motor owns the full tool sequence *within* a Task (reads the governing SKILL.md, composes its verbs, iterates); a Task making many tool calls is normal, and Motor fails only when the outcome needs a cross-envelope split (different specialty / approval gate / real phase dependency), which the daemon re-decomposes — Motor mints no envelopes (C-17). SOUL/SOUL_APPEND files carry operating character + how-to-find-skills only, **zero tool syntax** (B-16/B-17); per-skill procedure lives in SKILL.md. `checkpoint_plan` re-plan nudge forbids finer decomposition; `checkpoint-executor` no-verdict paths are now fail-closed (re-verify once, then FAIL — not needs_review-and-pass); missing `accept_criteria` falls back to an outcome-framed default.

### I/O Architecture
- `agent-ears` — deterministic input (poll, dedup, GChat preprocessor, fire-and-forget gateway POST)
- `agent-mouth` — output classification + delivery (JSONL-native transcript tailer, turn state machine, LLM status updates)
- Agents never call delivery tools directly; `agent-mouth` polls for `delivery_status=pending` envelopes (fallback to 3-status query)

## Key Files
- `corekit/config/chat-config.json.tmpl` — Chat adapter config template
- `corekit/config/agent-registry.json` — Per-agent settings (routes, models, intents, tools)
- `corekit/config/agent-types.json` — Available fleet agent specialties
- `platform/organ-firmware/prime/cortex/` — Prime brain: plan executor + synthesizer
- `platform/organ-firmware/prime/temporal-research/` — Prime brain: web search
- `platform/organ-firmware/prime/temporal-memory/` — Prime brain: memory recall
- `platform/organ-firmware/prime/prefrontal/` — Prime brain: planning + dispatch
- `platform/organ-firmware/prime/motor/` — Prime brain: execution
- `platform/organ-firmware/prime/cerebellum/` — Prime brain: verification
- `platform/organ-firmware/fleet/_base/` — Fleet: generic template (fallback)
- `platform/organ-firmware/fleet/_brain/` — Fleet: shared sub-agent workspaces
- `specialties/<type>/workspace/` — Fleet specialty workspaces (assistant, data, devops, engineer, finance, pm, qa, security)

## Memory System (Three-Layer Lifecycle)
- **Working Memory** (`MEMORY.md`): Agent RAM — accumulates during sessions, pruned nightly to <2,000 chars
- **Core Memory** (Firestore `core_memory` collection): Long-term durable facts. Actively pruned via `core-memory-retire`
  - Scripts: `core-memory-read` (supports `--since` time-windowed queries), `core-memory-write`, `core-memory-retire`
- **Deep Truths** (`SOUL.md` `## Deep Truths` section): Behavioral firmware, max 10 items. Changes only during nightly consolidation (3+ sessions, 7+ day evidence)
  - Script: `update-deep-truths`
- **Dual-pass recall**: temporal-memory does targeted archive search (all time) + broad recent scan (30 days) + context fill
- **Nightly consolidation**: 10-step `r-memory-consolidation` responsibility (gather → triage → reconcile → retire → promote → prune → Deep Truths → report)

## Gateway Integration Points
- Gateway API: `POST http://localhost:18789/v1/chat/completions` with `model: "brain"` or `model: "brain/<agentId>"`
- `agent-ears` bridges input channels → neural gateway (fire-and-forget POST)
- `agent-mouth` bridges gateway output → delivery channel (GChat or Firestore)
- Fleet tools (`fleet-deploy`, etc.) available via VM execution on PATH


## Session Context Layer (2026-07-11)
The brain's context handling is measured, cached, bounded, and thread-true:
- `platform/context/prompt-blocks.mjs` — cache-tier block assembly (boot/mission/volatile); the gateway maps tiers to Anthropic cache_control breakpoints (1h TTL) and concatenates for Gemini.
- `platform/context/compaction.mjs` — checkpoint digests + token-triggered mission compaction ("roll at N%"): `compactMissionContext` in agent-brain.mjs splices a validated digest (verbatim criteria, binned claims) into `_accumulated_context` at `contracts.compaction` thresholds.
- `platform/work/thread-ledger.mjs` — (channel, thread)-keyed conversation cache written at intake claim / GChat backfill / delivery; read by classify fill-in and mouth voicing.
- `corekit/brain/context.mjs` — gateway session store (dark; `session.enabled`); cortex decide sessions send volatile-tier deltas; cerebellum structurally excluded (B-28).
Debugging: grep daemon logs for `[TELEMETRY] llm_usage` (cache splits), `compaction`, `session_open|session_miss`, `thread_turn_append`. Gateway `/status` lists live sessions.
