{
  "id": "p-legal-review",
  "name": "Legal Review",
  "description": "Performs a legal review on a document, proposes redline modifications with reasoning, and generates a structured summary document.",
  "status": "active",
  "version": 8,
  "visibility": "standard",
  "intent_keywords": ["legal", "review", "redlines", "contract", "terms"],
  "parameters": {
    "document_id": {
      "description": "The Google Doc ID to review",
      "required": true
    },
    "project_id": {
      "description": "Project context",
      "required": false,
      "default": ""
    }
  },
  "steps": [
    {
      "title": "Analyze Document",
      "agent": "motor",
      "type": "standard",
      "intent": "research",
      "description": "Access the target document ${document_id} and analyze its contents for legal risks, structural concerns, clarity, and opportunities for redline improvements.",
      "accept_criteria": "The document ${document_id} was successfully retrieved and analyzed for risks, concerns, and proposed redlines."
    },
    {
      "title": "Propose Edits & Explanations",
      "agent": "motor",
      "type": "standard",
      "intent": "execute",
      "description": "Draft and apply proposed changes to the document for review. You MUST use the `docs-tab-clone` -> create `suggestions.json` -> `docs-tab-suggest` workflow described in SKILL.md. Do NOT use `docs-write --append`. Your suggestions.json should include the legal reasoning for each edit.",
      "accept_criteria": "A suggestion tab has been cloned, and `docs-tab-suggest` was successfully run using a `suggestions.json` file."
    },
    {
      "title": "Notify Reviewer",
      "agent": "motor",
      "type": "standard",
      "intent": "execute",
      "description": "Leave a notification comment on the document directing the reviewer to the suggestion tab. Wait for their response or approval.",
      "accept_criteria": "A comment notifying the reviewer is posted on the document, instructing them to strike through any yellow highlighted text they wish to reject."
    },
    {
      "title": "Finalize Accepted Edits",
      "agent": "motor",
      "type": "standard",
      "intent": "execute",
      "description": "Process the reviewer's feedback. You MUST use the `docs-tab-finalize` script, providing it with the exact same `--file suggestions.json` you used earlier. This will apply all approved modifications to the original document content, reject those struck through, delete the suggestion tab, and resolve the comment.",
      "accept_criteria": "`docs-tab-finalize` was successfully run with the `--file suggestions.json` parameter, and the original document is updated."
    },
    {
      "title": "Generate Summary Document",
      "agent": "motor",
      "type": "standard",
      "intent": "execute",
      "description": "Create a new document containing the final legal review summary. The summary document must adhere to the following structure:\n\n## Review Scope\n## Key Findings\n## Structural/Risk Concerns\n## Redline Summary",
      "accept_criteria": "A separate summary document has been created with the required structural headers (Review Scope, Key Findings, Structural/Risk Concerns, Redline Summary) filled out."
    },
    {
      "title": "Verify Execution",
      "agent": "motor",
      "type": "standard",
      "intent": "research",
      "description": "Verify that the final approved edits have been integrated, suggestion artifacts are removed, and a structured summary document with the required sections exists.",
      "accept_criteria": "Verification passed: The document is updated, temporary suggestion tabs/elements are gone, and the structured summary document exists."
    }
  ]
}

