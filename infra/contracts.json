{
  "_comment": "Single source of truth for all cross-cutting values. Read by validate-contracts, bootstraps, and runtime scripts. Edit THIS file \u2014 the manifest installs it to VMs.",
  "_budget_posture": "A bound exists to stop an UNBOUNDED blowup, not to save money. Agent success is worth far more than tokens, so every char/token budget here is sized for the work to succeed and only then bounded. The evidence runs both ways and both lessons are load-bearing: an uncapped readFile once pushed a single organ call to 877k input tokens (so caps stay, and every cap ANNOUNCES itself so an organ can re-derive instead of assuming it saw everything) — while a stingy cap silently truncated the Drive listing that held the right template, and the mission built three documents from the wrong one (so caps are generous). Subagent organs run on a 1,048,576-token context window; operating at 10% of it and paying for that in truncation-shaped failures was a bad trade. HARD CEILINGS THAT ARE NOT ABOUT COST, and must not be raised: dispatch.context_token_budget <= 125000 because _accumulated_context persists on the Firestore work document (1 MiB) and budget x 4 chars must fit inside it; compaction.working_budget_tokens <= dispatch.context_token_budget by the same argument. validate-contracts enforces both, and it caught a 500000 attempt made in exactly the generous spirit above — generosity stops where a storage limit starts. ONE ABSOLUTE RULE: nothing here may become unbounded. dispatch.verify_prompt_max_chars was briefly cut to 6000 to dodge a provider malformed-call threshold, and that was a cautionary tale: dodging one failure mode by starving evidence bought a worse one. But the REAL fix was neither a generous cap (24000 still hit MALFORMED at 13K) nor a stingy one — it was switching the evidence SOURCE from verbose motor output to compact result packets, which contain the tool results the verifier needs at a fraction of the size. The cap is now 9000 and first-attempt evidence is compact; full motor output is reserved for the missing-evidence re-verify path, bounded dynamically. See _verify_prompt_comment.",
  "version": 2,
  "vertex": {
    "location": "us-central1",
    "anthropicLocation": "us-east5",
    "models": {
      "cortex": "vertex-anthropic/claude-opus-4-6",
      "cortexFallback": "vertex-google/gemini-2.5-pro",
      "subagent": "vertex-google/gemini-2.5-flash"
    },
    "anthropic_prompt_caching": true,
    "anthropic_cache_ttl_stable": "1h",
    "anthropic_cache_ttl_mission": "1h",
    "anthropic_cache_message_breakpoints": true,
    "anthropic_session_affinity": true,
    "gemini_explicit_cache": false,
    "gemini_explicit_cache_ttl_seconds": 3600,
    "context_windows": {
      "claude-opus-4-6": 200000,
      "gemini-2.5-pro": 1048576,
      "gemini-2.5-flash": 1048576
    }
  },
  "agents": {
    "defaultId": "cortex",
    "subagentIds": [
      "temporal-research",
      "temporal-memory",
      "prefrontal",
      "motor",
      "cerebellum"
    ]
  },
  "gateway": {
    "port": 18789,
    "timeoutSeconds": 180,
    "bind": "loopback",
    "_loop_guard_comment": "Stuck-loop bounds. duplicate_* count identical ARGUMENTS (cannot distinguish a pointless repeat from legitimate polling). identical_result_* count byte-identical RESULTS for the same arguments — a proven no-op, so bounded tighter. semantic_* bound the varying-arguments case, where a lone nudge was demonstrably ignorable.",
    "duplicate_nudge_threshold": 3,
    "duplicate_terminate_threshold": 4,
    "identical_result_nudge_threshold": 2,
    "identical_result_terminate_threshold": 3,
    "semantic_nudge_threshold": 8,
    "semantic_terminate_threshold": 16
  },
  "tools": {
    "_comment": "Gateway tool-result discipline (B-4). A tool result IS context: bound it, ANNOUNCE the bound so the organ can re-derive instead of assuming it saw everything, and never let a binary file reach a model. An unbounded readFile on a downloaded PDF once pushed a single organ call to 877k input tokens.",
    "output_chars": 80000,
    "output_head_chars": 56000,
    "output_tail_chars": 20000,
    "timeout_ms": 120000,
    "max_buffer_bytes": 4194304,
    "binary_guard": true
  },
  "env": {
    "GOOGLE_GENAI_USE_VERTEXAI": "True",
    "GCE_METADATA_HOST": "metadata.google.internal"
  },
  "versioning": {
    "_comment": "Canonical commit format: v{YYYY}.{MM}.{DD}.{index}.{subindex}: description. This is the FOREVER format. The extractVersion() regex in app/src/app/api/upgrade/route.ts MUST match this. A temporary vX.Y.Z format was used during v5.0-v5.3 and is supported for back-compat only.",
    "commitFormat": "v{YYYY}.{MM}.{DD}.{index}.{subindex}: description",
    "canonicalRegex": "^(v\\d{4}\\.\\d{2}\\.\\d{2}\\.\\d+\\.\\d+)",
    "backcompatRegex": "^(v\\d+\\.\\d+\\.\\d+)"
  },
  "dispatch": {
    "model": "gemini-2.5-flash",
    "poll_interval_ms": 3000,
    "max_iterations": 50,
    "gateway_timeout_ms": 300000,
    "stale_cleanup_hours": 24,
    "archive_age_days": 7,
    "archive_interval_ms": 3600000,
    "context_token_budget": 125000,
    "needs_input_timeout_hours": 72,
    "step_ledger_enabled": true,
    "checkpoint_resume_enabled": true,
    "claim_stale_ms": 600000,
    "respond_enabled": true,
    "respond_reads_enabled": true,
    "respond_reads": [
      "fleet_status",
      "recent_work"
    ],
    "evidence_floor_exclude_skills": [
      "fleet-fire",
      "fleet-hire",
      "fleet-upgrade",
      "fleet-verify",
      "fleet-status",
      "fleet-deploy"
    ],
    "max_delegations_per_checkpoint": 4,
    "delegation_dedup_window_hours": 24,
    "result_preview_chars": 3000,
    "context_slice_chars": 2000,
    "ctx_verify_input": 60000,
    "ctx_verify_prior": 16000,
    "verify_prompt_max_chars": 12000,
    "_verify_full_evidence_comment": "Ceiling for the ONE re-verification triggered when a FAIL cites missing evidence OR when the executor KNOWS it fed the verifier a truncated view (compact > evidence budget). The executor now binds this knob under a prompt-safe budget (verify_prompt_max_chars - criteria - boilerplate) and reduces the evidence shape-aware (packToolEvidence keeps every tool RESULT, not a head+tail clip that drops the middle). Prior bug: a hardcoded '9000 - criteria' cap made this value DEAD (it never bound) and starved the re-verify to ~6.5K — the same truncation that caused the FAIL. Raising it above what the verifier model can ingest is a contract + model change (Gemini Flash MALFORMED_FUNCTION_CALL), not a code change.",
    "verify_full_evidence_max_chars": 80000,
    "synth_verification_enabled": true,
    "synth_verify_min_chars": 400,
    "_recovered_tool_error_comment": "detectMotorFailure matches a failure pattern (e.g. 'command failed') ANYWHERE in the motor's output. A multi-command task can have one sub-command error and still finish with the full deliverable in its prose answer — hard-failing that blocked a discovery mission that had gathered everything. When true, a failure marker confined to the [TOOL EXECUTION LOG] (with a substantive, non-failing answer beside it) is annotated for cerebellum to arbitrate (B-28), not hard-failed. When false, the strict any-substring hard-fail returns. A failure marker in the ANSWER prose, or a thin/absent answer, still hard-fails either way.",
    "recovered_tool_error_soft_fail": true,
    "_verify_prompt_comment": "Lowered 24000 -> 9000 and switched the first attempt from full motor output to compact result packets. The previous 24000 cap produced prompts of ~13K chars that hit MALFORMED_FUNCTION_CALL deterministically. The 6000 over-correction starved evidence because it used the SAME verbose motor output — the real fix is a different evidence SOURCE, not a different cap. Compact evidence (result packets, already capped at ctx_agent_step=8000/task) contains the tool results the verifier actually needs; the verbose motor reasoning is noise for verification. Full motor output is still available in the missing-evidence re-verify path, bounded to stay under the MALFORMED threshold.",
    "_spine_comment": "A checkpoint failure re-plans that checkpoint, not the mission. Outcomes are stable in practice (four observed missions never changed them) while task lists churn — and re-deriving the whole plan once re-ran a checkpoint that had PASSED twenty seconds earlier, at a cost of 1.11M input tokens. spine_pinning holds outcomes+verdicts; criteria_pinning holds the wording, allowing max_criteria_revisions refinements (evidence both ways: one mission passed only after a rewording, another reworded four times without converging). Cortex can still force a full re-shape via replan_scope:'mission' — logged as spine_replaced.",
    "spine_pinning_enabled": true,
    "criteria_pinning_enabled": true,
    "max_criteria_revisions": 1,
    "_pass_replan_comment": "Two guards against re-planning verified work — the elision churn where a read-only discovery re-shaped its already-PASSED checkpoint 5+ times (52 calls / 1.4M tokens) because its data rode as a ref Cortex never hydrated. pass_synthesize_nudge: after a passed plan, steer Cortex to synthesize (or hydrate a ref), where today success gets no nudge. reject_full_replan_when_passing: refuse a replan_scope:'mission' re-shape when every checkpoint passed and none failed — deliver or escalate, never re-shape verified work. Both revert to prior behavior when false.",
    "pass_synthesize_nudge_enabled": true,
    "reject_full_replan_when_passing": true,
    "_blocked_blocker_comment": "A milestone-verification verdict judges a checkpoint, not the mission's deliverable. A real report_fail (not merely inconclusive) on a checkpoint whose TASKS all succeeded once terminated a FINISHED mission as `blocked`, with the mission's own success summary written into the blocker field — a complete 2-page flyer PDF delivered to the operator as an on_failure outcome. When true: before finalizing an M-type mission as `blocked` (blocked.mjs), and before the self-unblock upgrade in synthesize_with_failure, the daemon checks whether any real dispatched TASK actually failed — excluding cerebellum `*.verify` pseudo-steps and inconclusive/timed-out rows (the same exclusion synthesize.mjs applies). If none failed and a deliverable exists, `blocked` is a mislabel and the mission is re-routed ONCE to a verified synthesize (B-28) instead. A genuine external blocker still blocks: a failed task, or an explicit non-'other' blocker_type (access/permission/auth/missing-input/external/dependency/quota/rate-limit). One-shot + iteration-bounded. Reverts to prior behavior when false.",
    "blocked_requires_real_blocker": true,
    "goal_state_enabled": true,
    "goal_check_required": true,
    "wait_min_minutes": 1,
    "wait_max_minutes": 1440,
    "wait_default_minutes": 5,
    "_verify_probe_comment": "DORMANT — not read by the executor. A probe lets a verifier ask for evidence it lacks, but nothing ever serviced a returned request (extractProbes was imported and never called), so advertising request_probe produced verifiers that asked a question we could not answer, and the B-28 fail-closed then failed the milestone for asking it. The scaffolding is out of the verify prompt until the probe loop exists; these flags are kept so re-enabling is one change, and left here rather than deleted so the intent is not lost.",
    "verify_probe_enabled": false,
    "verify_probe_max": 2,
    "verify_probe_stakes_min": "consequential",
    "attack_duty_stakes_min": "consequential",
    "min_deliverable_chars": 1
  },
  "ears": {
    "firestore_poll_ms": 3000,
    "gchat_poll_ms": 5000,
    "dedup_window_ms": 300000,
    "cooldown_ms": 2000,
    "gchat_context_messages": 0,
    "max_pages_per_poll": 5,
    "new_space_seed": "now",
    "mention_match": "annotation",
    "preprocess": {
      "enabled": true,
      "model": "gemini-2.5-flash",
      "maxTokens": 2000,
      "temperature": 0.0
    }
  },
  "mouth": {
    "llm_enabled": true,
    "model": "gemini-2.5-flash",
    "maxTokens": 2000,
    "temperature": 0.1,
    "fallback": "deliver_raw",
    "source": "jsonl",
    "dashboard_visibility_mirror": true,
    "status_updates": {
      "enabled": true,
      "schedule_ms": [
        10000,
        300000,
        600000,
        1800000,
        7200000
      ]
    }
  },
  "chat": {
    "reply_in_thread": true
  },
  "delivery": {
    "_comment": "Mouth delivery retry discipline: exponential backoff between failed attempts, terminal delivery_status='failed' at max_attempts. Brain fast-fails waiting delegation Ts off the terminal status.",
    "max_attempts": 8,
    "retry_backoff_base_seconds": 5,
    "retry_backoff_max_seconds": 600
  },
  "conversation": {
    "enabled": true,
    "max_turns": 12,
    "budget_chars": 20000,
    "per_turn_chars": 2000,
    "voicing_enabled": true,
    "voicing_tail_chars": 1500,
    "thread_ledger_enabled": true,
    "turn_store_chars": 24000,
    "compact_after_turns": 30,
    "summary_max_chars": 1200,
    "turn_retention_days": 14,
    "voicing_ledger_enabled": true
  },
  "compaction": {
    "enabled": true,
    "checkpoint_digest_chars": 12000,
    "trigger_pct": 0.7,
    "working_budget_tokens": 100000,
    "min_compactable_tokens": 8000,
    "keep_recent_iterations": 5,
    "max_compactions_per_mission": 3,
    "digest_max_chars": 16000,
    "session_log_to_git": false,
    "learnings_max_entries": 5
  },
  "session": {
    "enabled": true,
    "idle_ttl_minutes": 30,
    "max_sessions": 16,
    "miss_circuit_breaker": 3,
    "excluded_agents": ["cerebellum"]
  },
  "organ_context": {
    "result_store_enabled": true,
    "packet_summary_chars": 4000,
    "list_summary_top_k": 8,
    "_tool_answer_min_prose_comment": "For a tool-agent result, prose OUTSIDE the [TOOL EXECUTION LOG] below this many chars means the tool RESULTS are the answer (a discovery mission), so the packet summary digests those results instead of eliding the log to a bare marker. Above it (an edit confirmation, an audit verdict), the prose is the answer and the log is elided for economy. The defect this gates: a 2.6KB discovery result collapsed to a 53-char '[tool log elided]' marker, so Cortex saw no data and re-planned.",
    "tool_answer_min_prose_chars": 240,
    "hydrate_enabled": true,
    "hydrate_max_chars": 48000,
    "hydrate_max_refs_per_mission": 16,
    "verifier_full_evidence": true,
    "loop_guard_synthesize": true,
    "blackboard_enabled": true,
    "blackboard_max_chars": 40000
  },
  "projects": {
    "context_max_tokens": 2000,
    "promotion_auto": false,
    "archive_completed_after_days": 30
  },
  "utility": {
    "model": "gemini-2.5-flash",
    "location": "us-central1",
    "maxTokens": {
      "summarize": 1024,
      "title": 60,
      "schema": 8192,
      "notification": 2048
    },
    "temperature": {
      "summarize": 0.3,
      "title": 0.3,
      "schema": 0.1,
      "notification": 0.2
    },
    "context_budgets": {
      "agent_step": 24000,
      "cortex_step": 12000,
      "dispatch_success": 12000,
      "dispatch_failure": 12000
    },
    "timeout_ms": 30000,
    "enforce_schema_timeout_ms": 15000,
    "enforce_schema_max_attempts": 2,
    "disable_thinking": true
  },
  "memory": {
    "_comment": "Memory subsystem knobs. resource_ledger: an external identifier (Drive folder/doc id, chat space) is a fact about the world that does not change — resolve it once, remember it, never search again. Capture is deterministic from structured tool output (C-4/C-5) and lands on the mission envelope in Firestore, so it survives a resume that re-clones and destroys the working tree. Surfacing is memory's job: recall Layer E offers it to temporal-memory, which remains the sole producer of the packet the brain sees.",
    "resource_ledger": {
      "enabled": true,
      "max_entries": 200,
      "recall_limit": 40,
      "_promotion": "Promotion into the durable `resources` category is TEMPORAL-MEMORY's job, done during consolidation (memory-consolidate SKILL step 5b) — not a daemon auto-write. v2026.06.21.1.0 deliberately removed daemon writes to core memory as noise, and temporal-memory is the memory authority and sole memory mutator. The daemon only captures to the envelope; within-mission and across-resume reuse works from there immediately, and cross-mission reuse lands at the next consolidation."
    }
  },
  "workspaces": {
    "_comment": "Canonical workspace directories the daemon reads from. Manifests MUST target these paths.",
    "cortex": "workspace",
    "motor": "workspace-motor",
    "cerebellum": "workspace-cerebellum",
    "prefrontal": "workspace-prefrontal",
    "temporal-research": "workspace-temporal-research",
    "temporal-memory": "workspace-temporal-memory"
  },
  "git": {
    "_comment": "Tenant-internal git artifact substrate. Single bucket, per-repo prefixes. ADC via metadata SA token.",
    "bucket": "${TENANT}-agent-git",
    "prefix": "git/",
    "artifacts_prefix": "artifacts/",
    "max_artifact_mb": 20,
    "defaultBranch": "main",
    "maxPushRetries": 5,
    "gcBundleThreshold": 25
  },
  "github": {
    "owner": "YOUR_GITHUB_ORG",
    "repo": "architect-prime-gcp-agent",
    "tokenSecret": "github-token"
  }
}